
<tbody>
    <tr>
      <th scope="row"><%= link_to room.title, room_path(room.title), target: "_blank", rel: "noopener noreferrer" %></th>
      <td><%= room.messages.count %></td>
      <td><%= room.user.email %></td>
      <td><%= room.created_at %></td>
    </tr>
  </tbody>



